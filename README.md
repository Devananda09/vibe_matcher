# vibematcher

Small project for matching vibes — add description here.

## Notes
- Do NOT commit secrets (e.g. `.env`, API keys). Keep them in environment variables.
- The notebook `vibe_matcher.ipynb` will attempt to install `openai` if missing.
